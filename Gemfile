# This Gemfile pins the site to the same Jekyll setup that GitHub Pages
# uses on its servers, so what you see locally matches what gets
# published. You shouldn't need to edit this file.

source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "webrick"  # needed to run the local preview server on newer Ruby versions
